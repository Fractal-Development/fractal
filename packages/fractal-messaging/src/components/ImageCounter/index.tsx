import React from 'react';

type Props = {
    imageIndex: number;
    imagesCount: number;
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function ImageCounter(_props: Props): JSX.Element {
    return <></>;
}
