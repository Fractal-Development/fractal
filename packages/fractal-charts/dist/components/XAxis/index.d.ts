import { XAxisProps } from '../../types';
export declare function XAxis({ contentInset, xAccessor, scale, formatLabel, style, data, numberOfTicks, textProps, min, max, color, ...layerProps }: XAxisProps): JSX.Element;
