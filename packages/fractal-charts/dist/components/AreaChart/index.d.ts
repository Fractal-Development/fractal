import { AreaChartProps } from '../../types';
export declare function AreaChart({ style, areaColor, lineColor, lineWidth, dotRadius, gradientConfig, curve, start, data, contentInset, xAccessor, yAccessor, yScale, xScale, numberOfTicks, gridMax, gridMin, clampX, clampY, yMin, yMax, xMin, xMax, ...layerProps }: AreaChartProps): JSX.Element;
