import { ComponentRouteProps } from '../types/ComponentRouteProps';
import React from 'react';

export function AuthenticateSection({ children }: ComponentRouteProps): JSX.Element {
    return <>{children}</>;
}
